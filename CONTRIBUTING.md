# 参与贡献

1. 从 `main` 创建功能分支；
2. 不提交运行数据、密钥、数据库、日志和编译产物；
3. Windows 客户端版本必须与根目录 `VERSION` 一致；
4. 提交前运行客户端本地构建，或至少通过 GitHub Actions 的 Validate source；
5. Shell、Python、YAML 使用 UTF-8 与 LF；PowerShell/C# 可使用 CRLF；
6. 正式版本通过新语义版本标签发布，不覆盖已有 Release。
