<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:av="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="av" x:Class="SplinterCellCNLauncher.MainWindow"
        Title="SplinterCellCNLauncher Public"
        Icon="/logo.ico"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        TextOptions.TextHintingMode="Fixed"
        FontFamily="Microsoft YaHei UI"
        Height="720"
        Width="1200"
        MinHeight="720"
        MinWidth="1200"
        ResizeMode="NoResize"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded"
        Closing="Window_Closing"
        Background="#050607">
    <Window.Resources>
        <SolidColorBrush x:Key="WindowBrush" Color="#050607"/>
        <SolidColorBrush x:Key="CardBrush" Color="#E60A0C0F"/>
        <SolidColorBrush x:Key="CardLightBrush" Color="#CC111419"/>
        <SolidColorBrush x:Key="CardBorderBrush" Color="#42FFFFFF"/>
        <SolidColorBrush x:Key="InputBrush" Color="#12000000"/>
        <SolidColorBrush x:Key="InputBorderBrush" Color="#88FFFFFF"/>
        <SolidColorBrush x:Key="InputFocusBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextMainBrush" Color="#F2F2F0"/>
        <SolidColorBrush x:Key="TextSubBrush" Color="#B7B9BA"/>
        <SolidColorBrush x:Key="TextMutedBrush" Color="#777B7E"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#F2F2F0"/>
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="AccentSoftBrush" Color="#2BFFFFFF"/>
        <SolidColorBrush x:Key="DangerSoftBrush" Color="#3B1C20"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#E54858"/>
        <SolidColorBrush x:Key="DangerHoverBrush" Color="#F05C6A"/>
        <SolidColorBrush x:Key="BlueSoftBrush" Color="#13253B"/>

        <DropShadowEffect x:Key="CardShadow"
                          Color="#000000"
                          BlurRadius="24"
                          ShadowDepth="8"
                          Opacity="0.28"/>

        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{StaticResource TextMainBrush}"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei UI"/>
        </Style>

        <Style x:Key="CaptionText" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{StaticResource TextSubBrush}"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei UI"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style x:Key="FieldLabel" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{StaticResource TextSubBrush}"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="2,0,0,7"/>
        </Style>

        <Style TargetType="{x:Type TextBox}">
            <Setter Property="Height" Value="44"/>
            <Setter Property="Foreground" Value="{StaticResource TextMainBrush}"/>
            <Setter Property="Background" Value="{StaticResource InputBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="CaretBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="SelectionBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="11">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource InputFocusBrush}"/>
                                <Setter TargetName="border" Property="Background" Value="#0B1722"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.52"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type PasswordBox}">
            <Setter Property="Height" Value="44"/>
            <Setter Property="Foreground" Value="{StaticResource TextMainBrush}"/>
            <Setter Property="Background" Value="{StaticResource InputBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="CaretBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="SelectionBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type PasswordBox}">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="11">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource InputFocusBrush}"/>
                                <Setter TargetName="border" Property="Background" Value="#0B1722"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.52"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CenterTextBox" TargetType="{x:Type TextBox}" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,0"/>
        </Style>

        <Style x:Key="CenterPasswordBox" TargetType="{x:Type PasswordBox}" BasedOn="{StaticResource {x:Type PasswordBox}}">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,0"/>
        </Style>

        <Style x:Key="SmallButton" TargetType="{x:Type Button}">
            <Setter Property="Height" Value="31"/>
            <Setter Property="Foreground" Value="#E4F0FF"/>
            <Setter Property="Background" Value="#26000000"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei UI"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="13,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="9"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#28FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#16FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.42"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="{x:Type Button}">
            <Setter Property="Height" Value="54"/>
            <Setter Property="MinWidth" Value="250"/>
            <Setter Property="Foreground" Value="{StaticResource TextMainBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="17"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei UI"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Grid x:Name="buttonRoot" Background="Transparent">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ContentPresenter VerticalAlignment="Center"
                                              HorizontalAlignment="Left"/>
                            <TextBlock Grid.Column="1"
                                       Text="→"
                                       FontSize="25"
                                       FontWeight="Light"
                                       Margin="22,0,0,1"
                                       VerticalAlignment="Center"/>
                            <Border x:Name="underline"
                                    Grid.ColumnSpan="2"
                                    Height="1"
                                    Background="#CCFFFFFF"
                                    VerticalAlignment="Bottom"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="underline" Property="Height" Value="2"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="buttonRoot" Property="Opacity" Value="0.68"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="buttonRoot" Property="Opacity" Value="0.35"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style x:Key="DangerButton" TargetType="{x:Type Button}" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Foreground" Value="#FF7B82"/>
        </Style>

        <Style x:Key="DialogPrimaryButton" TargetType="{x:Type Button}">
            <Setter Property="MinWidth" Value="72"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Foreground" Value="#021108"/>
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei UI"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2DC869"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.42"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DialogSecondaryButton" TargetType="{x:Type Button}">
            <Setter Property="MinWidth" Value="72"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Foreground" Value="#E4F0FF"/>
            <Setter Property="Background" Value="#26000000"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei UI"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#28FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#16FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.42"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style x:Key="WindowChromeButton" TargetType="{x:Type Button}">
            <Setter Property="Width" Value="34"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Foreground" Value="#D7E6F7"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei UI"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#243850"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#19283A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>



        <Style x:Key="TitleBarTextButton" TargetType="{x:Type Button}" BasedOn="{StaticResource WindowChromeButton}">
            <Setter Property="Width" Value="76"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="8,0"/>
        </Style>

        <Style x:Key="WindowChromeCloseButton" TargetType="{x:Type Button}" BasedOn="{StaticResource WindowChromeButton}">
            <Setter Property="Foreground" Value="#FFDDE1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource DangerBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#C73C4B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SettingsMenuItemStyle" TargetType="{x:Type MenuItem}">
            <Setter Property="Foreground" Value="{StaticResource TextMainBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type MenuItem}">
                        <Border x:Name="menuBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="13,9"
                                Margin="4,2">
                            <ContentPresenter ContentSource="Header"
                                              RecognizesAccessKey="False"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="menuBorder" Property="Background" Value="#26FFFFFF"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SettingsContextMenuStyle" TargetType="{x:Type ContextMenu}">
            <Setter Property="MinWidth" Value="190"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ContextMenu}">
                        <Border Background="#F20A0C0F"
                                BorderBrush="#55FFFFFF"
                                BorderThickness="1"
                                CornerRadius="10"
                                Padding="0,5"
                                SnapsToDevicePixels="True">
                            <Border.Effect>
                                <DropShadowEffect Color="#000000"
                                                  BlurRadius="18"
                                                  ShadowDepth="5"
                                                  Opacity="0.48"/>
                            </Border.Effect>
                            <StackPanel IsItemsHost="True"
                                        KeyboardNavigation.DirectionalNavigation="Cycle"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModeListBox" TargetType="{x:Type ListBox}">
            <Setter Property="Height" Value="46"/>
            <Setter Property="Width" Value="184"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="{x:Type ListBoxItem}">
                        <Setter Property="Foreground" Value="#7F8385"/>
                        <Setter Property="FontFamily" Value="Microsoft YaHei UI"/>
                        <Setter Property="FontSize" Value="13"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                    <Grid x:Name="itemRoot" Background="Transparent" Margin="0,0,28,0">
                                        <ContentPresenter HorizontalAlignment="Left"
                                                          VerticalAlignment="Center"/>
                                        <Border x:Name="itemUnderline"
                                                Height="1"
                                                Width="40"
                                                HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                Background="Transparent"/>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="#DADADA"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="itemUnderline" Property="Background" Value="#FFFFFF"/>
                                            <Setter Property="Foreground" Value="#FFFFFF"/>
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter TargetName="itemRoot" Property="Opacity" Value="0.45"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBox}">
                        <Border Background="Transparent" Padding="0">
                            <ItemsPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="2"/>
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CinematicTextBox" TargetType="{x:Type TextBox}">
            <Setter Property="Height" Value="43"/>
            <Setter Property="Foreground" Value="{StaticResource TextMainBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="#90FFFFFF"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Padding" Value="1,0,1,4"/>
            <Setter Property="CaretBrush" Value="#FFFFFF"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="SelectionBrush" Value="#66FFFFFF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="fieldBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="fieldBorder" Property="BorderBrush" Value="#FFFFFF"/>
                                <Setter TargetName="fieldBorder" Property="BorderThickness" Value="0,0,0,2"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="fieldBorder" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CinematicPasswordBox" TargetType="{x:Type PasswordBox}">
            <Setter Property="Height" Value="43"/>
            <Setter Property="Foreground" Value="{StaticResource TextMainBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="#90FFFFFF"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Padding" Value="1,0,1,4"/>
            <Setter Property="CaretBrush" Value="#FFFFFF"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="SelectionBrush" Value="#66FFFFFF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type PasswordBox}">
                        <Border x:Name="fieldBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="fieldBorder" Property="BorderBrush" Value="#FFFFFF"/>
                                <Setter TargetName="fieldBorder" Property="BorderThickness" Value="0,0,0,2"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="fieldBorder" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CinematicUtilityButton" TargetType="{x:Type Button}" BasedOn="{StaticResource SmallButton}">
            <Setter Property="Foreground" Value="#BFC1C2"/>
            <Setter Property="Background" Value="#1A000000"/>
            <Setter Property="BorderBrush" Value="#38FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="Normal"/>
        </Style>
    </Window.Resources>

    <Window.Triggers>
        <EventTrigger RoutedEvent="FrameworkElement.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetName="backgroundImage"
                                     Storyboard.TargetProperty="Opacity"
                                     From="0" To="1" Duration="0:0:0.8"/>
                    <DoubleAnimation Storyboard.TargetName="heroPanel"
                                     Storyboard.TargetProperty="Opacity"
                                     From="0" To="1" BeginTime="0:0:0.12" Duration="0:0:0.55"/>
                    <DoubleAnimation Storyboard.TargetName="heroRevealTransform"
                                     Storyboard.TargetProperty="Y"
                                     From="16" To="0" BeginTime="0:0:0.12" Duration="0:0:0.55">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                    <DoubleAnimation Storyboard.TargetName="authPanel"
                                     Storyboard.TargetProperty="Opacity"
                                     From="0" To="1" BeginTime="0:0:0.2" Duration="0:0:0.5"/>
                    <DoubleAnimation Storyboard.TargetName="authRevealTransform"
                                     Storyboard.TargetProperty="X"
                                     From="24" To="0" BeginTime="0:0:0.2" Duration="0:0:0.5">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <Grid x:Name="rootGrid" Background="{StaticResource WindowBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="76"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Image x:Name="backgroundImage"
               Grid.RowSpan="2"
               Opacity="0"
               Source="Assets/scbl-launcher-background.png"
               Stretch="UniformToFill"
               RenderOptions.BitmapScalingMode="HighQuality"/>
        <MediaElement x:Name="backgroundVideo"
                      Grid.RowSpan="2"
                      Opacity="0"
                      Stretch="UniformToFill"
                      IsMuted="True"
                      LoadedBehavior="Manual"
                      UnloadedBehavior="Manual"
                      ScrubbingEnabled="False"
                      MediaOpened="BackgroundVideo_MediaOpened"
                      MediaEnded="BackgroundVideo_MediaEnded"
                      MediaFailed="BackgroundVideo_MediaFailed"/>
        <Border Grid.RowSpan="2" Background="#1F000000"/>
        <Border Grid.RowSpan="2">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                    <GradientStop Color="#2E000000" Offset="0"/>
                    <GradientStop Color="#05000000" Offset="0.48"/>
                    <GradientStop Color="#96000000" Offset="0.72"/>
                    <GradientStop Color="#E6000000" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
        </Border>

        <Border Grid.Row="0" Panel.ZIndex="10" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#B3050607" Offset="0"/>
                    <GradientStop Color="#33050607" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Grid Margin="34,0,170,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="#163B29"
                            BorderBrush="#245B3E"
                            BorderThickness="1"
                            CornerRadius="999"
                            Padding="10,4"
                            HorizontalAlignment="Left"
                            Margin="0,0,0,12"
                            Visibility="Collapsed">
                        <TextBlock x:Name="txtTopBadge" Text="CN PRIVATE SERVER"
                                   Foreground="#7DFFAE"
                                   FontSize="11"
                                   FontWeight="Bold"
                                   />
                    </Border>

                    <TextBlock x:Name="txtAppTitle" Text="SCBL CONNECT"
                               FontSize="17"
                               FontWeight="SemiBold"
                               Foreground="#F7FBFF"
                               VerticalAlignment="Center"
                               Margin="0,0,34,0"/>

                    <Border Width="1" Height="22" Background="#55FFFFFF" Margin="0,0,30,0"/>

                    <Border x:Name="bdAnnouncementClip"
                            Width="520"
                            Height="22"
                            HorizontalAlignment="Left"
                            ClipToBounds="True"
                            MouseEnter="AnnouncementClip_MouseEnter"
                            MouseLeave="AnnouncementClip_MouseLeave"
                            SizeChanged="AnnouncementClip_SizeChanged">
                        <Canvas x:Name="announcementCanvas" Height="22">
                            <TextBlock x:Name="txtAppSubtitle"
                                       Text="OK兄弟们，干起来♂"
                                       FontSize="12"
                                       Foreground="{StaticResource TextSubBrush}"
                                       TextWrapping="NoWrap"
                                       Opacity="0.76"
                                       Canvas.Top="2">
                                <TextBlock.RenderTransform>
                                    <TranslateTransform x:Name="announcementTransform" X="0"/>
                                </TextBlock.RenderTransform>
                            </TextBlock>
                        </Canvas>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <StackPanel x:Name="spTitleButtons"
                    Grid.Row="0"
                    Panel.ZIndex="20"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Margin="0,22,25,0">
            <Button x:Name="btnSettings"
                    Content="⚙"
                    Style="{StaticResource WindowChromeButton}"
                    Click="SettingsButton_Click"
                    Margin="0,0,4,0">
                <Button.ContextMenu>
                    <ContextMenu x:Name="settingsMenu"
                                 Placement="Bottom"
                                 HorizontalOffset="-150"
                                 Style="{StaticResource SettingsContextMenuStyle}"
                                 Opened="SettingsMenu_Opened">
                        <MenuItem x:Name="miGuide"
                                  Header="❓ 使用指引"
                                  Style="{StaticResource SettingsMenuItemStyle}"
                                  Click="GuideButton_Click"/>
                        <MenuItem x:Name="miLanguageToggle"
                                  Header="🌐 中文 / English"
                                  Style="{StaticResource SettingsMenuItemStyle}"
                                  Click="LanguageToggleButton_Click"/>
                        <MenuItem x:Name="miMusicToggle"
                                  Header="🔊 声音：开启"
                                  Style="{StaticResource SettingsMenuItemStyle}"
                                  Click="MusicToggleButton_Click"/>
                        <Separator Margin="9,4" Background="#35FFFFFF"/>
                        <MenuItem x:Name="miServerSettings"
                                  Header="🖧 服务器设置"
                                  Style="{StaticResource SettingsMenuItemStyle}"
                                  Click="ServerSettingsMenuItem_Click"/>
                        <Separator Margin="9,4" Background="#35FFFFFF"/>
                        <MenuItem x:Name="miOverwriteSaves"
                                  Header="💾 覆盖全解锁存档"
                                  Style="{StaticResource SettingsMenuItemStyle}"
                                  Click="OverwriteSavesMenuItem_Click"/>
                        <MenuItem x:Name="miRepairNetwork"
                                  Header="🛠 修复网络"
                                  Style="{StaticResource SettingsMenuItemStyle}"
                                  Click="RepairNetworkMenuItem_Click"/>
                        <MenuItem x:Name="miExportDiagnostics"
                                  Header="📦 导出诊断信息"
                                  Style="{StaticResource SettingsMenuItemStyle}"
                                  Click="ExportDiagnosticsMenuItem_Click"/>
                    </ContextMenu>
                </Button.ContextMenu>
            </Button>
            <Button x:Name="btnWindowMinimize"
                    Content="—"
                    Style="{StaticResource WindowChromeButton}"
                    Click="WindowMinimizeButton_Click"/>
            <Button x:Name="btnWindowClose"
                    Content="×"
                    Style="{StaticResource WindowChromeCloseButton}"
                    Click="WindowCloseButton_Click"
                    Margin="4,0,0,0"/>
        </StackPanel>


        <StackPanel x:Name="heroPanel"
                    Grid.Row="1"
                    Panel.ZIndex="4"
                    Opacity="0"
                    Width="430"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Bottom"
                    Margin="72,0,0,72">
            <StackPanel.RenderTransform>
                <TranslateTransform x:Name="heroRevealTransform" Y="16"/>
            </StackPanel.RenderTransform>
            <TextBlock Text="SCBL / FIFTH ECHELON"
                       Foreground="#8F9293"
                       FontSize="11"
                       FontWeight="SemiBold"
                       Margin="2,0,0,13"/>
            <TextBlock x:Name="txtHeroHeadline"
                       Text="准备进入第五梯队"
                       Foreground="#D9D9D6"
                       FontSize="42"
                       FontWeight="Light"
                       TextOptions.TextFormattingMode="Ideal"/>
            <Border Width="86"
                    Height="2"
                    Background="#E6FFFFFF"
                    HorizontalAlignment="Left"
                    Margin="2,19,0,0"/>
        </StackPanel>

        <Border x:Name="authPanel"
                Grid.RowSpan="2"
                Width="390"
                Opacity="0"
                Margin="0,106,72,68"
                HorizontalAlignment="Right"
                VerticalAlignment="Stretch"
                Panel.ZIndex="4"
                Background="Transparent"
                BorderThickness="0"
                Padding="0">
            <Border.RenderTransform>
                <TranslateTransform x:Name="authRevealTransform" X="24"/>
            </Border.RenderTransform>
            <StackPanel>
                <Grid Margin="0,0,0,22">
                    <TextBlock x:Name="txtSectionTitle"
                               FontSize="18"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center"><Run Language="zh-cn" Text="基本信息"/></TextBlock>
                    <Button x:Name="btnPlayers"
                            HorizontalAlignment="Right"
                            MinWidth="128"
                            Height="31"
                            Content="当前在线玩家：1"
                            Style="{StaticResource CinematicUtilityButton}"
                            Click="PlayersButton_Click"/>
                </Grid>



                <!-- 公网专版不再显示 RADMIN/服务器输入/主机发现相关控件。
                     公网入口、虚拟服务端地址均由后台固定配置。 -->

                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock x:Name="txtUsernameLabel" Text="账号" Style="{StaticResource FieldLabel}"/>
                        <TextBox x:Name="txtUsername" Style="{StaticResource CinematicTextBox}"
                                 InputMethod.IsInputMethodEnabled="False"
                                 InputMethod.PreferredImeState="Off"
                                 InputScope="AlphanumericHalfWidth"
                                 PreviewTextInput="UsernameTextBox_PreviewTextInput"
                                 DataObject.Pasting="UsernameTextBox_Pasting"
                                 TextChanged="UsernameTextBox_TextChanged"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock x:Name="txtPasswordLabel" Text="密码" Style="{StaticResource FieldLabel}"/>
                        <PasswordBox x:Name="txtPassword" Style="{StaticResource CinematicPasswordBox}"
                                     PreviewTextInput="PasswordBox_PreviewTextInput"
                                     DataObject.Pasting="PasswordBox_Pasting"/>
                    </StackPanel>
                </Grid>
                <Border x:Name="bdStatusPanel" Background="#14000000"
                        BorderBrush="#35FFFFFF"
                        BorderThickness="0,0,0,1"
                        Padding="0,0,0,14"
                        Margin="0,0,0,20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Column="0"
                                Grid.RowSpan="2"
                                Width="24"
                                Height="24"
                                Background="#22000000"
                                BorderBrush="#55FFFFFF"
                                BorderThickness="1"
                                CornerRadius="12"
                                Margin="0,0,10,0"
                                VerticalAlignment="Center">
                            <Ellipse x:Name="serverStatusLight"
                                     Width="10"
                                     Height="10"
                                     Fill="Gray"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center"/>
                        </Border>

                        <Grid Grid.Column="1" Grid.Row="0">
                            <TextBlock x:Name="txtConnectionStatusCaption" Text="连接状态"
                                       Foreground="{StaticResource TextMutedBrush}"
                                       FontSize="10"
                                       VerticalAlignment="Center"/>
                            <Button x:Name="btnCheckNetwork"
                                    HorizontalAlignment="Right"
                                    MinWidth="74"
                                    Height="27"
                                    Padding="9,0"
                                    Content="↻ 检测网络"
                                    Style="{StaticResource CinematicUtilityButton}"
                                    Click="CheckNetworkButton_Click"
                                    Margin="8,0,0,0"/>
                        </Grid>

                        <TextBlock x:Name="txtServerStatus"
                                   Grid.Column="1"
                                   Grid.Row="1"
                                   Text="未检测"
                                   Foreground="#E3E3E1"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Margin="0,2,0,0"
                                   TextWrapping="NoWrap"
                                   TextTrimming="CharacterEllipsis"/>
                    </Grid>
                </Border>

                <TextBlock x:Name="txtLaunchModeLabel" Text="启动模式" Style="{StaticResource FieldLabel}" Margin="0,0,0,5"/>
                <ListBox x:Name="cmbGameExecutable"
                         Style="{StaticResource ModeListBox}"
                         SelectedIndex="0"
                         Margin="0,0,0,18">
                    <ListBoxItem Content="DX9"/>
                    <ListBoxItem Content="DX11"/>
                </ListBox>

                <Button x:Name="btnLaunch"
                        Content="启动游戏"
                        Style="{StaticResource PrimaryButton}"
                        Click="LaunchButton_Click"
                        HorizontalAlignment="Stretch"
                        Margin="0,0,0,14"/>
            </StackPanel>
        </Border>
        <TextBlock x:Name="txtFooterNotice"
                   Visibility="Collapsed"
                   Style="{StaticResource FieldLabel}" 
                   Margin="39,339,40,0" 
                   Grid.Row="1"
                   FontWeight="Bold"
                   VerticalAlignment="Top"
                   TextWrapping="WrapWithOverflow"
                   ><Run Text="友情提示： 本启动器基于开源项目 5th Echelon 优化制作，"/><Run Text="一键接入，方便游玩。"/><LineBreak/><Run/><Run Text="原项目地址：https://github.com/unixoide/5th-echelon "/><LineBreak/><Run/><LineBreak/><Run Text="国内联机交流群：709112052 "/><LineBreak/><Run Text="等你来♂战！"/></TextBlock>

        <TextBlock x:Name="txtLauncherVersion"
                   Grid.Row="1"
                   Panel.ZIndex="4"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Bottom"
                   Margin="72,0,0,28"
                   Foreground="#858889"
                   FontSize="10"
                   FontWeight="SemiBold"
                   Text="公网专版 v0.3.7"/>


        <Grid x:Name="playersOverlay"
              Grid.RowSpan="2"
              Panel.ZIndex="80"
              Visibility="Collapsed"
              Background="#66000000">
            <Border Width="520"
                    MaxHeight="520"
                    Background="#F20A0C0F"
                    BorderBrush="#55FFFFFF"
                    BorderThickness="1"
                    CornerRadius="18"
                    Padding="22"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect Color="#000000"
                                      BlurRadius="28"
                                      ShadowDepth="8"
                                      Opacity="0.45"/>
                </Border.Effect>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock x:Name="txtPlayersTitle"
                               Grid.Row="0"
                               Text="当前在线玩家"
                               Foreground="{StaticResource TextMainBrush}"
                               FontSize="17"
                               FontWeight="Bold"
                               Margin="0,0,0,14"/>

                    <Grid Grid.Row="1" Margin="10,0,10,7">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="112"/>
                            <ColumnDefinition Width="72"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Name="txtPeerHeaderName"
                                   Grid.Column="0"
                                   Text="玩家ID"
                                   Foreground="{StaticResource TextMutedBrush}"
                                   FontSize="10"
                                   FontWeight="SemiBold"/>
                        <TextBlock x:Name="txtPeerHeaderIp"
                                   Grid.Column="1"
                                   Text="虚拟IP"
                                   Foreground="{StaticResource TextMutedBrush}"
                                   FontSize="10"
                                   FontWeight="SemiBold"
                                   TextAlignment="Center"/>
                        <TextBlock x:Name="txtPeerHeaderLatency"
                                   Grid.Column="2"
                                   Text="延迟"
                                   Foreground="{StaticResource TextMutedBrush}"
                                   FontSize="10"
                                   FontWeight="SemiBold"
                                   TextAlignment="Right"/>
                    </Grid>

                    <ScrollViewer Grid.Row="2"
                                  MaxHeight="330"
                                  VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="spPeerList"/>
                    </ScrollViewer>

                    <StackPanel Grid.Row="3"
                                Orientation="Horizontal"
                                HorizontalAlignment="Right"
                                Margin="0,16,0,0">
                        <Button x:Name="btnClosePlayers"
                                MinWidth="72"
                                Content="取消"
                                Style="{StaticResource DialogSecondaryButton}"
                                Click="ClosePlayersButton_Click"
                                Margin="0,0,10,0"/>
                        <Button x:Name="btnRefreshPlayers"
                                MinWidth="72"
                                Content="刷新"
                                Style="{StaticResource DialogPrimaryButton}"
                                Click="RefreshPlayersButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <Grid x:Name="dialogOverlay"
              Grid.RowSpan="2"
              Visibility="Collapsed"
              Background="#66000000">
            <Border Width="520"
                    MaxHeight="520"
                    Background="#F20A0C0F"
                    BorderBrush="#55FFFFFF"
                    BorderThickness="1"
                    CornerRadius="18"
                    Padding="22"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect Color="#000000"
                                      BlurRadius="28"
                                      ShadowDepth="8"
                                      Opacity="0.45"/>
                </Border.Effect>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock x:Name="txtDialogTitle"
                               Grid.Row="0"
                               Text="提示"
                               Foreground="{StaticResource TextMainBrush}"
                               FontSize="17"
                               FontWeight="Bold"
                               Margin="0,0,0,14"/>

                    <ScrollViewer Grid.Row="1"
                                  MaxHeight="330"
                                  VerticalScrollBarVisibility="Auto">
                        <TextBlock x:Name="txtDialogMessage"
                                   Text="消息内容"
                                   Foreground="{StaticResource TextSubBrush}"
                                   FontSize="13"
                                   LineHeight="22"
                                   TextWrapping="Wrap"
                                   Margin="0,0,8,0"/>
                    </ScrollViewer>

                    <StackPanel Grid.Row="2"
                                Orientation="Horizontal"
                                HorizontalAlignment="Right"
                                Margin="0,16,0,0">
                        <Button x:Name="btnDialogNo"
                                MinWidth="72"
                                Content="取消"
                                Style="{StaticResource DialogSecondaryButton}"
                                Click="DialogNoButton_Click"
                                Margin="0,0,10,0"/>
                        <Button x:Name="btnDialogYes"
                                MinWidth="72"
                                Content="确定"
                                Style="{StaticResource DialogPrimaryButton}"
                                Click="DialogYesButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <Grid x:Name="serverSettingsOverlay"
              Grid.RowSpan="2"
              Panel.ZIndex="80"
              Visibility="Collapsed"
              Background="#88000000">
            <Border Width="480"
                    Background="#F20A0C0F"
                    BorderBrush="#55FFFFFF"
                    BorderThickness="1"
                    CornerRadius="18"
                    Padding="22"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect Color="#000000"
                                      BlurRadius="28"
                                      ShadowDepth="8"
                                      Opacity="0.48"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock x:Name="txtServerSettingsTitle"
                               Text="服务器设置"
                               FontSize="17"
                               FontWeight="Bold"
                               Margin="0,0,0,7"/>
                    <TextBlock x:Name="txtServerSettingsDescription"
                               Text="修改服务器域名或 IP。设置将在下次启动时生效。"
                               Style="{StaticResource CaptionText}"
                               TextWrapping="Wrap"
                               LineHeight="19"
                               Margin="0,0,0,17"/>

                    <TextBlock x:Name="txtServerEndpointLabel"
                               Text="服务器地址（域名/IP:端口）"
                               Style="{StaticResource FieldLabel}"/>
                    <TextBox x:Name="txtServerEndpoint"
                             Height="41"
                             Margin="0,0,0,13"/>

                    <TextBlock x:Name="txtServerUpdatePortLabel"
                               Text="更新服务端口"
                               Style="{StaticResource FieldLabel}"/>
                    <TextBox x:Name="txtServerUpdatePort"
                             Height="41"
                             PreviewTextInput="ServerPort_PreviewTextInput"
                             DataObject.Pasting="ServerPort_Pasting"/>

                    <TextBlock x:Name="txtServerSettingsMessage"
                               MinHeight="20"
                               Foreground="#F6A7AF"
                               FontSize="12"
                               TextWrapping="Wrap"
                               Margin="2,10,0,0"/>

                    <Grid Margin="0,15,0,0">
                        <Button x:Name="btnServerSettingsReset"
                                HorizontalAlignment="Left"
                                MinWidth="86"
                                Content="恢复默认"
                                Style="{StaticResource DialogSecondaryButton}"
                                Click="ServerSettingsResetButton_Click"/>
                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Right">
                            <Button x:Name="btnServerSettingsCancel"
                                    MinWidth="72"
                                    Content="取消"
                                    Style="{StaticResource DialogSecondaryButton}"
                                    Click="ServerSettingsCancelButton_Click"
                                    Margin="0,0,10,0"/>
                            <Button x:Name="btnServerSettingsSave"
                                    MinWidth="72"
                                    Content="保存"
                                    Style="{StaticResource DialogPrimaryButton}"
                                    Click="ServerSettingsSaveButton_Click"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>

        <Canvas x:Name="guideOverlay"
                Grid.RowSpan="2"
                Panel.ZIndex="90"
                Visibility="Collapsed"
                Background="#66000000">
            <Border x:Name="guideHighlight"
                    BorderBrush="#7DFFAE"
                    BorderThickness="2"
                    CornerRadius="14"
                    Background="#18FFFFFF"
                    IsHitTestVisible="False"/>

            <TextBlock x:Name="guideArrow"
                       Text="▲"
                       Foreground="#7DFFAE"
                       FontSize="18"
                       FontWeight="Bold"
                       Panel.ZIndex="91"
                       IsHitTestVisible="False"/>

            <Border x:Name="guideCard"
                    Width="330"
                    Background="#121F2D"
                    BorderBrush="#3E6284"
                    BorderThickness="1"
                    CornerRadius="16"
                    Padding="14">
                <Border.Effect>
                    <DropShadowEffect Color="#000000"
                                      BlurRadius="30"
                                      ShadowDepth="8"
                                      Opacity="0.5"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock x:Name="txtGuideStep"
                               Text="1 / 6"
                               Foreground="#7DFFAE"
                               FontSize="11"
                               FontWeight="Bold"
                               Margin="0,0,0,6"/>
                    <TextBlock x:Name="txtGuideTitle"
                               Text="指引标题"
                               Foreground="{StaticResource TextMainBrush}"
                               FontSize="17"
                               FontWeight="Bold"
                               Margin="0,0,0,10"/>
                    <TextBlock x:Name="txtGuideMessage"
                               Text="指引内容"
                               Foreground="{StaticResource TextSubBrush}"
                               FontSize="12.3"
                               LineHeight="19"
                               TextWrapping="Wrap"
                               Margin="0,0,0,14"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="btnGuideSkip"
                                Content="跳过"
                                Width="62"
                                Style="{StaticResource DialogSecondaryButton}"
                                Click="GuideSkipButton_Click"
                                Margin="0,0,8,0"/>
                        <Button x:Name="btnGuidePrev"
                                Content="上一步"
                                Width="72"
                                Style="{StaticResource DialogSecondaryButton}"
                                Click="GuidePrevButton_Click"
                                Margin="0,0,8,0"/>
                        <Button x:Name="btnGuideNext"
                                Content="下一步"
                                Width="76"
                                Style="{StaticResource DialogPrimaryButton}"
                                Click="GuideNextButton_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Canvas>

    </Grid>
</Window>
