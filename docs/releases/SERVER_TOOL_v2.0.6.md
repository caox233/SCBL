# SCBL Server 2.0.6

正式二进制由 GitHub Actions 构建并发布，生产服务器不编译源码。
客户端单组件支持 GitHub 在线下载，并继续由服务端校验和发布。

- 修复发布客户端或更新公告后，更新清单错误保留 `root` 用户组的问题。
- 更新清单现在始终归属 `scbl-update:scbl`，使控制面可以读取当前正式客户端版本。
- 防止客户端已经更新成功后，启动游戏仍被错误提示版本不一致。
